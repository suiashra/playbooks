---
- name: Creating the users.
  user:
    name: "{{ sname }}"
    password: "{{ 'Abc123' | password_hash('sha512') }}"
    comment: "{{ sfull }}"
    shell: /usr/bin/bash
    group: wheel
    groups: wheel
    home: /home/{{ sname }} |
